fastapi
uvicorn
httpx
httpcore
PyMySQL
pydantic
pydantic-settings
python-multipart 
passlib           # For password hashing (e.g., bcrypt backend)
python-jose[cryptography] # For JWT (FastAPI standard)
PyJWT             # Alternative/redundant, keep one if needed. (python-jose is better)
python-dotenv     # For local development .env files
requests          # If you make external API calls
anyio             # Async I/O backend
sniffio           # Async library tool
sqlalchemy        # ORM for database interactions
alembic           # For database migrations